[package]
name = "fl-index"
description = "Reader for stock astrometry.net index files: mmap, libkd kd-trees, quads"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
fl-fits = { workspace = true }
memmap2 = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# deterministic PRNG for golden tests, no external deps
